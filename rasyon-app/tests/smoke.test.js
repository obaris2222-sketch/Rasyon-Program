// Quick test runner diagnostic
import { describe, it, expect } from 'vitest';
import { calcAARequirements } from '../src/core/aminoAcids.js';

describe('quick smoke', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
